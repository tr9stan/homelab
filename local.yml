---
- name: Update and upgrade Ubuntu servers
  hosts: all
  connection: local
  become: yes
  become_user: root

  tasks:
    #- name: Update package cache
     # apt:
      #  update_cache: yes

    - name: Install nala package
      apt:
        name: nala
        state: present
